<%= form_with(model: bokmark) do |form| %>
  <% if bokmark.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bokmark.errors.count, "error") %> prohibited this bokmark from being saved:</h2>

      <ul>
        <% bokmark.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
